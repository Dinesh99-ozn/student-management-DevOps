# Shared environment variables used by docker-compose.yml
# Copy this file to .env before running `docker compose up`.

DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=student_management
JWT_SECRET=change_this_secret_in_production
JWT_EXPIRES_IN=8h
